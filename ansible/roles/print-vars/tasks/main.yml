---
- name: Make sure the role is running
  debug:
    msg: "Hello, World!"
- name: See if something from all.yml comes
  debug:
    var: root_pw
- name: See if something from localhost.yml comes
  debug:
    var: server_details['switch_name']
- name: Print something about the individual host
  debug:
    var: id