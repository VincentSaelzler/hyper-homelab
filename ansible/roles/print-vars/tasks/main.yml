---
- name: Make sure the role is running
  debug:
    msg: "Hello, World!"
- name: See if something from all.yml comes
  debug:
    var: allvar
- name: See if something from localhost.yml comes
  debug:
    var: hostvar